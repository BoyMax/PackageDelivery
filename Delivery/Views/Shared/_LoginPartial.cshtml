@using Microsoft.AspNet.Identity
@*@if (Request.IsAuthenticated)*@
@*@if(Session["LoginId"]!=null)
{*@
    @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @*@Html.AntiForgeryToken()*@

        <ul class="nav navbar-nav navbar-right">
            @*<li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>*@
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="glyphicon glyphicon-user" style="color:white"></span>  
                @if (Session["LoginName"] != null)
                {
                @Session["LoginName"].ToString()
                }
                <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                    <li class="dropdown-header">订单信息</li>
                    <li>@Html.ActionLink("我发出的订单", "Index", "Orders")</li>
                    <li>@Html.ActionLink("我接收的订单", "ReceiveIndex", "Orders")</li>
                    <li class="divider"></li>
                    <li class="dropdown-header">个人信息</li>
                    <li>@Html.ActionLink("个人设置", "PersonalSetting", "Users")</li>
                </ul>
            </li>
            <li>@Html.ActionLink("注销", "Login", "Account", routeValues: null, htmlAttributes: new { @class = "glyphicon glyphicon-off" })</li>
        </ul>

    }
@*}*@
@*else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("注册", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("登录", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
     </ul>
}*@
